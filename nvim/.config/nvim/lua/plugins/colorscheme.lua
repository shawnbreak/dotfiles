return {
    'folke/tokyonight.nvim',
    lazy = false,
    priority = 1000,
    enabled = true,
    config = function(opts)
        -- require("tokyonight").setup({
        --     transparent = true
        -- })
        -- vim.cmd([[colorscheme tokyonight]])
    end
}
